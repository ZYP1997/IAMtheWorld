INCLUDE_DIRECTORIES(..)

SET (LIBRARY_OUTPUT_PATH ../../lib)

AUX_SOURCE_DIRECTORY(./src SRC_DIR)

LINK_DIRECTORIES (../lib)

ADD_LIBRARY (Time SHARED ${SRC_DIR})

TARGET_LINK_LIBRARIES(Time libObject.dll)