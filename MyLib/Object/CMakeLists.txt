CMAKE_MINIMUM_REQUIRED (VERSION 3.10)

PROJECT (MyLib)

INCLUDE_DIRECTORIES(./include)

SET (LIBRARY_OUTPUT_PATH ../../lib)

AUX_SOURCE_DIRECTORY(./src SRC_DIR)

ADD_LIBRARY (Object SHARED ${SRC_DIR})

#cmake .. -G "MinGW Makefiles"